#title:dockAt
#author:zozoh
#index:0,1
--------------------------------------------------------------------
dockAt 概述

    将一个元素浮动到一个目标元素附近
    {{{
    LT     MT      RT
    L  $(selector) R
    LB     MB      RB
    }}}

--------------------------------------------------------------------
dockAt 的使用方式

    {{{
    $(selector).dockAt(ele|jq|selector, 'lt|rt|...', 20);
    }}}

